import styled from "styled-components";

// 4lip
export const Description = (props) => {
  console.log("elo1");
  return <StyledWraper>Filip </StyledWraper>;
};
const StyledWraper = styled("p")(()=>({backgroundColor: "red"}))